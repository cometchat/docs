---
title: "Conversations"
description: "Learn how to integrate and customize the CometChatConversations widget for displaying user conversations with advanced styling and functionality options."
---

<Info>
The `CometChatConversations` widget displays all conversations related to the currently logged-in user with comprehensive customization options.
</Info>

## Overview

The `CometChatConversations` is a [Widget](/ui-kit/flutter/components-overview#components), That shows all conversations related to the currently logged-in user,

<Frame caption="Conversations Widget" alt="CometChat conversations widget showing user conversations">
  <img src="/images/68da96e3-conversations-22a7ffeca154c79c33e12b924a516447.png" />
</Frame>

## Usage

<Steps>
<Step title="Integration">
As `CometChatConversations` is a widget, it can be initiated either by tapping a button or through the trigger of any event. It offers multiple parameters and methods for tailoring its user interface.

You can launch `CometChatConversations` directly using `Navigator.push`, or you can define it as a widget within the `build` method of your `State` class.
</Step>
</Steps>

<CardGroup cols={2}>
<Card title="Using Navigator" icon="navigation">
Launch `CometChatConversations` directly using `Navigator.push`.
</Card>
<Card title="Embedding as Widget" icon="layers">
Define it as a widget within the `build` method of your `State` class.
</Card>
</CardGroup>

<Tabs>
<Tab title="Dart">
```dart
Navigator.push(context, MaterialPageRoute(builder: (context) => const CometChatConversations()));
```
</Tab>
</Tabs>

<Tabs>
<Tab title="Dart">
```dart
import 'package:cometchat_chat_uikit/cometchat_chat_uikit.dart';
import 'package:flutter/material.dart';

class Conversations extends StatefulWidget {
  const Conversations({super.key});

  @override
  State<Conversations> createState() => _ConversationsState();
}

class _ConversationsState extends State<Conversations> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: CometChatConversations(),
      ),
    );
  }
}
```
</Tab>
</Tabs>

## Actions

<AccordionGroup>
<Accordion title="onItemTap" icon="mouse-pointer">
`onItemTap` is triggered when you click on a ListItem of the `CometChatConversations` widget. This `onItemTap` method proves beneficial when a user intends to customize the click behavior in CometChatConversations.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onItemTap: (conversation) => {
        // TODO("Not yet implemented")
    }
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="onItemLongPress" icon="hand">
`onItemLongPress` is triggered when you on long press on a ListItem of the `CometChatConversations` widget. This `onItemLongPress` method proves beneficial when a user intends to customize the long press behavior in CometChatConversations.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onItemLongPress: (conversation) => {
         // Handle long press on conversation item
    }
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="onBack" icon="arrow-left">
This `onBack` method becomes valuable when a user needs to override the action triggered upon pressing the back button in CometChatConversations.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onBack: () => {
        // TODO("Not yet implemented")
    },
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="setOnSelection" icon="check-square">
The `onSelection` feature enables selection with modes: `SelectionMode.single` and `SelectionMode.multiple`.

The `onSelection` event is triggered upon the completion of a selection in `onSelection`. This returns the selected conversations list when the callback is triggered. It can be executed with any button or action.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    selectionMode: SelectionMode.multiple,
    onSelection: (list) => {
        // TODO("Not yet implemented")
    },
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="onError" icon="alert-triangle">
This method proves helpful when a user needs to customize the action taken upon encountering an error in CometChatConversations.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onError: (e) {
        // TODO("Not yet implemented")
    },
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="onLoad" icon="download">
Invoked when the list is successfully fetched and loaded, helping track component readiness.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onLoad: (list) {
        // Handle conversations list load
    },
),
```
</Tab>
</Tabs>
</Accordion>

<Accordion title="onEmpty" icon="slash">
Called when the list is empty, enabling custom handling such as showing a placeholder message

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    onEmpty: () {
        // Handle empty conversations list
    },
),
```
</Tab>
</Tabs>
</Accordion>
</AccordionGroup>

## Filters

<Info>
You can set `ConversationsRequestBuilder` in the `CometChatConversations` widget to filter the conversation list. You can modify the builder as per your specific requirements with multiple options available to know more refer to [ConversationsRequestBuilder](/sdk/flutter/retrieve-conversations).
</Info>

<CardGroup cols={2}>
<Card title="Conversation Type" icon="users">
Filters on type of Conversation, User or Groups.
</Card>
<Card title="Limit" icon="list">
Number of conversations fetched in a single request.
</Card>
<Card title="WithTags" icon="tag">
Filter on fetching conversations containing tags.
</Card>
<Card title="Tags" icon="hash">
Filters on specific Tag.
</Card>
<Card title="UserTags" icon="user">
Filters on specific User Tag.
</Card>
<Card title="GroupTags" icon="users">
Filters on specific Group Tag.
</Card>
</CardGroup>

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    conversationsRequestBuilder: ConversationsRequestBuilder()
        ..conversationType = "user"
        ..limit = 10
        ..withTags = false,
)
```
</Tab>
</Tabs>

<CardGroup cols={2}>
<Card title="Build" icon="hammer">
Builds and returns an `ConversationsRequest` object.
</Card>
<Card title="Conversation Type" icon="message-square">
Type of the conversation.
</Card>
<Card title="Limit" icon="list">
Number of results to limit the query.
</Card>
<Card title="Tags" icon="hash">
Tags for filtering.
</Card>
<Card title="With Tags" icon="tag">
Flag to include tags.
</Card>
<Card title="With User And Group Tags" icon="users">
Flag to include user and group tags.
</Card>
</CardGroup>

## Events

<AccordionGroup>
<Accordion title="Conversation Deleted" icon="trash">
This `ccConversationDeleted` will be emitted when the user deletes a conversation

<Tabs>
<Tab title="Dart">
```dart
import 'package:cometchat_chat_uikit/cometchat_chat_uikit.dart';

class _YourScreenState extends State<YourScreen> with CometChatConversationEventListener {

  @override
  void initState() {
    super.initState();
    CometChatConversationEvents.addConversationListListener("listenerId", this); // Add the listener
  }

  @override
  void dispose(){
    super.dispose();
    CometChatConversationEvents.removeConversationListListener("listenerId"); // Remove the listener
  }

  @override
  void ccConversationDeleted(Conversation conversation) {
    // TODO("Not yet implemented")
  }
}
```
</Tab>
</Tabs>
</Accordion>
</AccordionGroup>

## Customization

<Info>
To align with your app's design specifications, you have the flexibility to customize the appearance of the conversation widget. We offer accessible methods that empower you to tailor the experience and functionality to meet your unique requirements.
</Info>

<AccordionGroup>
<Accordion title="Style" icon="palette">
You can set the `CometChatConversationsStyle` to the `CometChatConversations` Widget to customize the styling.

<Tabs>
<Tab title="Dart">
```dart
CometChatConversations(
    conversationsStyle: CometChatConversationsStyle(
              avatarStyle: CometChatAvatarStyle(
                borderRadius: BorderRadius.circular(8),
                backgroundColor: Color(0xFFFBAA75),
              ),
              badgeStyle:CometChatBadgeStyle(
                backgroundColor: Color(0xFFF76808)
              )
)
),
```
</Tab>
</Tabs>

<Frame caption="Conversation Styling" alt="Styled conversation widget">
  <img src="/images/d0ef0279-conversation_styling-22ac86dbef32a65c2320f06f70301560.png" />
</Frame>
</Accordion>

<Accordion title="Functionality" icon="settings">
These are a set of small functional customizations that allow you to fine-tune the overall experience of the widget. With these, you can change text, set custom icons, and toggle the visibility of UI elements.

<Frame caption="Conversations Functionality" alt="Conversations widget with functionality options">
  <img src="/images/68da96e3-conversations-22a7ffeca154c79c33e12b924a516447.png" />
</Frame>

<Tabs>
<Tab title="Dart">
```dart
 CometChatConversations(
    showBackButton: false,
    title: "Your Title",
)
```
</Tab>
</Tabs>

<Tip>
The widget exposes many properties for customization including selection, app bar options, scroll control, date patterns, state views, and more.
</Tip>
</Accordion>

<Accordion title="Advanced Customization" icon="zap">
For advanced-level customization, you can set custom views to the widget. This lets you tailor each aspect of the widget to fit your exact needs and application aesthetics.

<CardGroup cols={2}>
<Card title="setOptions" icon="settings">
Sets predefined list of actions for long press on conversations.
</Card>
<Card title="addOptions" icon="plus">
Extends existing actions without removing default ones.
</Card>
<Card title="disableSoundForMessages" icon="volume-x">
Disables sound notifications for incoming messages.
</Card>
<Card title="setCustomSoundForMessages" icon="volume-2">
Enables custom sound files for message notifications.
</Card>
<Card title="loadingStateView" icon="loader">
Sets custom loading view for data fetching.
</Card>
<Card title="emptyStateView" icon="slash">
Configures custom view for empty conversation lists.
</Card>
<Card title="errorStateView" icon="alert-triangle">
Defines custom error state view for issues.
</Card>
<Card title="leadingView" icon="user">
Custom leading view for conversation items.
</Card>
<Card title="titleView" icon="type">
Overrides default title view in conversation list.
</Card>
<Card title="trailingView" icon="arrow-right">
Customizes trailing view of conversation items.
</Card>
<Card title="ListItemView" icon="list">
Assigns custom ListItem view to the widget.
</Card>
<Card title="TextFormatters" icon="edit-3">
Assigns list of text formatters for message bubbles.
</Card>
<Card title="AppBarOptions" icon="menu">
Sets custom app bar options for the widget.
</Card>
<Card title="DatePattern" icon="calendar">
Modifies date pattern to custom requirements.
</Card>
<Card title="SubtitleView" icon="text">
Customizes subtitle view for conversation items.
</Card>
</CardGroup>

<Check>
All advanced customization options provide flexibility to create unique, brand-specific conversation interfaces while maintaining functionality.
</Check>
</Accordion>
</AccordionGroup>

<Warning>
When implementing custom views and options, ensure they maintain accessibility standards and provide clear user feedback for all interactions.
</Warning>

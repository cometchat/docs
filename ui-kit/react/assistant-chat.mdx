---
title: "Assistant Chat"
---

## Overview

`CometChatAssistantChat` is a composite component that assembles the message header, message list, and message composer to provide an AI agent chat experience. It supports streaming responses, quick starter suggestions, "New Chat" to reset context, and a chat history sidebar.

{/* <Frame>
  <img src="/images/assistant_chat_overview.png" />
</Frame> */}

<Tabs>
<Tab title="AssistantChatDemo.tsx">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        // Replace with your assistant UID
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return(
        <>
            <CometChatAssistantChat user={agent} />
        </>
    ); 
}

```

</Tab>

<Tab title="App.tsx">
```tsx
import { AssistantChatDemo } from "./AssistantChatDemo";

export default function App() {
    return (
        <div className="App">
            <AssistantChatDemo />
        </div>
    );
}
```

</Tab>
</Tabs>

<Warning>
  A `CometChat.User` (the AI agent) is required to start the assistant chat.
</Warning>

---

## Actions

[Actions](/ui-kit/react/components-overview#actions) control how a component behaves. You can hook into the following callbacks:

##### 1. onBackButtonClicked

Called when the header back button is clicked (visible when `showBackButton` is true).

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleBack = () => {
        // your custom action
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            showBackButton
            onBackButtonClicked={handleBack}
        />
    ); 
}

```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleBack = () => {
        // your custom action
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat 
            user={agent} 
            showBackButton 
            onBackButtonClicked={handleBack} 
        />
    );
}
```

</Tab>
</Tabs>

##### 2. onCloseButtonClicked

Called when the header close button is clicked (visible when `showCloseButton` is true).

<Tabs>
<Tab title="TypeScript">
```tsx

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleClose = () => {
        // your custom action
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat 
            user={agent} 
            showBackButton 
            onCloseButtonClicked={handleClose} 
        />
    );
}
```

</Tab>

<Tab title="JavaScript">
```jsx
export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleClose = () => {
        // your custom action
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat 
            user={agent} 
            showBackButton 
            onCloseButtonClicked={handleClose} 
        />
    );
}
```

</Tab>
</Tabs>

##### 3. onError

Listen to errors from the underlying header, list, or composer.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";
export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleError = (error: CometChat.CometChatException) => {
        // your error handling
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat 
            user={agent} 
            onError={handleError} 
        />
    );
}
```
</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";
export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const handleError = (error: CometChat.CometChatException) => {
        // your error handling
    };

    if (!agent) return null;

    return (
        <CometChatAssistantChat 
            user={agent} 
            onError={handleError} 
        />
    )
}
```

</Tab>
</Tabs>

{/* <Note>
The header’s "New Chat" and "History" buttons are built-in. You can override them by providing a custom `headerAuxiliaryButtonView`.
</Note> */}

---

## Customization

To fit your app’s design requirements, you can customize the appearance of the Assistant Chat component. We provide exposed properties that allow you to modify the experience and behavior according to your specific needs.

### Style

You can set the css of the Assistant Chat Component to customize the styling.

{/* <Frame>
  <img src="/images/assistant_chat_style.png" />
</Frame> */}

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <div className="cometchat-assistant-chat__wrapper">
            <CometChatAssistantChat user={agent} />
        </div>
    );
}
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <div className="cometchat-assistant-chat__wrapper">
            <CometChatAssistantChat user={agent} />
        </div>
    );
}
```

</Tab>

<Tab title="css">
```css
.cometchat-assistant-chat__wrapper { 
    /* layout overrides */ 
}

.cometchat-assistant-chat__header-auxiliary-view { 
    display: flex; 
    gap: 8px; 
}

.cometchat-assistant-chat__empty-state {
    /* background, spacing */
}

.cometchat-assistant-chat__empty-state-title { 
    font-weight: 600; 
}

.cometchat-assistant-chat__empty-state-subtitle { 
    opacity: 0.8; 
}

.cometchat-assistant-chat__suggestion-pill { 
    border-radius: 999px; 
}
```

</Tab>
</Tabs>

### Functionality

These props tailor the experience. Most message options (copy/edit/delete/react, receipts, date separators, etc.) are disabled by default for assistant chats.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    // Example: Set streaming speed to 30 characters per second
    // and show close button
    // You can also customize suggestions, empty state, etc.
    return (
        <CometChatAssistantChat
            user={agent}
            showCloseButton={true}
            setStreamingSpeed={30}
        />
    )
}
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    // Example: Set streaming speed to 30 characters per second
    // and show close button
    // You can also customize suggestions, empty state, etc.
    return (
        <CometChatAssistantChat
            user={agent}
            showCloseButton={true}
            setStreamingSpeed={30}
        />
    );
}
```

</Tab>
</Tabs>

---

| Property                | Description                                                              | Example                             |
| ----------------------- | ------------------------------------------------------------------------ | ----------------------------------- |
| `user`                  | Required `CometChat.User` representing the AI agent.                     | `user={agent}`                      |
| `streamingSpeed`        | Characters-per-second speed for streaming replies. Default `30`.         | `streamingSpeed={50}`               |
| `typewriterDelay`       | Per-character delay control (reserved).                                  | `typewriterDelay={25}`              |
| `suggestions`           | Array of quick prompts for the empty state.                              | `suggestions=["Help", "Summarize"]` |
| `hideSuggestions`       | Hide the suggestions section.                                            | `hideSuggestions={true}`            |
| `hideNewChat`           | Hide the New Chat button in header.                                      | `hideNewChat={true}`                |
| `hideChatHistory`       | Hide the History button/sidebar.                                         | `hideChatHistory={true}`            |
| `showBackButton`        | Show back button in header.                                              | `showBackButton`                    |
| `showCloseButton`       | Show close button in header.                                             | `showCloseButton`                   |
| `onBackButtonClicked`   | Back button handler.                                                     | `onBackButtonClicked={() => {}}`    |
| `onCloseButtonClicked`  | Close button handler.                                                    | `onCloseButtonClicked={() => {}}`   |
| `onError`               | Error handler.                                                           | `onError={handleError}`             |
| `emptyView`             | Custom empty state for the list.                                         | `emptyView={<Empty/>}`              |
| `loadingView`           | Custom loading view.                                                     | `loadingView={<Loading/>}`          |
| `errorView`             | Custom error view.                                                       | `errorView={<Error/>}`              |
| `emptyChatTitleView`    | Custom empty title (default uses metadata.greetingMessage or user name). | `emptyChatTitleView={<h3/>}`        |
| `emptyChatSubtitleView` | Custom empty subtitle (default uses metadata.introductoryMessage).       | `emptyChatSubtitleView={<p/>}`      |
| `emptyChatImageView`    | Custom empty image.                                                      | `emptyChatImageView={<img/>}`       |
| `assistantTools`        | Provide tool/function handlers for the assistant.                        | `assistantTools={tools}`            |

### Advanced

#### Header Views

Customize header sections using the following props: `headerItemView`, `headerTitleView`, `headerSubtitleView`, `headerLeadingView`, `headerTrailingView`, and `headerAuxiliaryButtonView`.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat, CometChatButton } from "@cometchat/chat-uikit-react";
import notesIcon from "../../assets/notesIcon.svg";
import savedIcon from "../../assets/savedIcon.svg";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const getAuxiliaryView = () => (
        <div className="header-view">
            <CometChatButton iconURL={notesIcon} text="Notes" />
            <CometChatButton iconURL={savedIcon} text="Saved" />
        </div>
    );

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            showBackButton
            showCloseButton
            headerTitleView={<span>AI Assistant</span>}
            headerSubtitleView={<span>Ask anything…</span>}
            headerAuxiliaryButtonView={getAuxiliaryView()}
        />
    );
}

```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat, CometChatButton } from "@cometchat/chat-uikit-react";
import notesIcon from "../../assets/notesIcon.svg";
import savedIcon from "../../assets/savedIcon.svg";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const getAuxiliaryView = () => (
        <div className="header-view">
            <CometChatButton iconURL={notesIcon} text="Notes" />
            <CometChatButton iconURL={savedIcon} text="Saved" />
        </div>
    );

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            showBackButton
            showCloseButton
            headerTitleView={<span>AI Assistant</span>}
            headerSubtitleView={<span>Ask anything…</span>}
            headerAuxiliaryButtonView={getAuxiliaryView()}
        />
    );
}

```

</Tab>

<Tab title="css">
```css
.header-view {
    display: flex;
    gap: 6px;
}

.header-view .cometchat .cometchat-button {
    width: auto;
    height: 32px;
    border-radius: 1000px;
}
```

</Tab>
</Tabs>

#### Assistant Tools

Pass an instance of `CometChatAssistantTools` to enable tool/function calls during assistant replies.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";
import { CometChatAssistantTools } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const tools = new CometChatAssistantTools({
        getCurrentWeather: ({ location }: { location: string }) => {
            // call your backend or a public API
            console.log("Fetching weather for", location);
        },
        createTicket: ({ title }: { title: string }) => {
            // open your internal ticketing flow
            console.log("Create ticket:", title);
        },
    });

    if (!agent) return null;

    return (
        <CometChatAssistantChat user={agent} assistantTools={tools} />
    );
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";
import { CometChatAssistantTools } from "@cometchat/chat-uikit-react";

export const AssistantChatDemo = () => {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    const tools = new CometChatAssistantTools({
        getCurrentWeather: ({ location }: { location: string }) => {
            // call your backend or a public API
            console.log("Fetching weather for", location);
        },
        createTicket: ({ title }: { title: string }) => {
            // open your internal ticketing flow
            console.log("Create ticket:", title);
        },
    });

    if (!agent) return null;

    return (
        <CometChatAssistantChat user={agent} assistantTools={tools} />
    );
}
```

</Tab>
</Tabs>

#### Empty Chat Image View

Provide a custom image for the empty state using `emptyChatImageView`.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatImageView={<img alt="AI Assistant" src="/images/assistant.png" />}
        />
    );
}
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatImageView={<img alt="AI Assistant" src="/images/assistant.png" />}
        />
    )
}
```

</Tab>
</Tabs>

#### Empty Chat Title View

Override the empty state title with `emptyChatTitleView`.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatTitleView={<div>Hi, I am your personal AI Assistant!</div>}
        />
    );
}
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatTitleView={<div>Hi, I am your personal AI Assistant!</div>}
        />
    )
}
```

</Tab>
</Tabs>

#### Empty Chat Subtitle View

Override the empty state subtitle with `emptyChatSubtitleView`.

<Tabs>
<Tab title="TypeScript">
```tsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState<CometChat.User>();

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatSubtitleView={<div>Ask me anything to get started</div>}
        />
    );
}
```

</Tab>

<Tab title="JavaScript">
```jsx
import React from "react";
import { CometChat } from "@cometchat/chat-sdk-javascript";
import { CometChatAssistantChat } from "@cometchat/chat-uikit-react";

export function AssistantChatDemo() {
    const [agent, setAgent] = React.useState(null);

    React.useEffect(() => {
        CometChat.getUser("assistant_uid").then((u) => setAgent(u));
    }, []);

    if (!agent) return null;

    return (
        <CometChatAssistantChat
            user={agent}
            emptyChatSubtitleView={<div>Ask me anything to get started</div>}
        />
    );
}
```

</Tab>
</Tabs>

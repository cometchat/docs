---
openapi: post /users/{uid}/auth_tokens
description: "Create a CometChat auth token for a user UID so client apps can log users in through SDKs without exposing credentials."
---

For the complete error reference, see [Error Guide](/articles/error-guide).
